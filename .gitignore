# Arquivos de conexão e credenciais - NUNCA enviar ao GitHub
conecta_mysql.inc
conecta_mysql*.inc
*.env
config.php

# valida_sessao.inc É versionado (contém só o define("Versao", ...), sem
# credenciais). Precisa entrar no Git para o cache-buster dos JS/CSS subir
# junto com as alterações. O ambiente de cada servidor pode ter o número
# ajustado manualmente no deploy via FTP.

# Pastas desnecessárias
vendor-copia/
planilhas_excel - Copia/
phpmailer - Copia/
vendor - Copia/

# Arquivos temporários
*.tmp
*.log
*.bak
Thumbs.db
.DS_Store

# Backups/dumps e arquivos de editor que não fazem parte do código do sistema
sistema.zip
*.sql
sftp-config.json
objects_salary.txt
pai.csv
teste_style.html
